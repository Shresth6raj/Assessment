<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
	<link rel="stylesheet" type="text/css" href="/stylesheets/sign_up.css">
<body style="background-image: url(/images/signup.jpeg); backdrop-filter: blur(2px);background-size: cover; background-repeat: no-repeat; background-position: center"></body>
<script type="text/javascript">
        function check(){
        var x=/\w+@(gmail.com|yahoo.com|hotmail.com)$/;
        var y=document.getElementById("email").value;
        var p=document.getElementById("name").value;
        var a=document.getElementById("password").value;
        var b=document.getElementById("cnf").value;
        var c=document.getElementById("phone").value;
        var d=document.getElementById("dob").value;
        var age=new Date(d);
        var date= new Date();
        var c_age=date.getFullYear()-age.getFullYear();

        if(x.test(y)==false){
         return alert("Invalid E-mail");

        }
        if(p==""){
            return alert("Name can't be NULL!");
        }
        if(a!=b){
         return alert("PASSWORD didn't match!");

        }

        if(/^\d{10}$/.test(c)==false){
         return alert("Invalid phone no!");

        }

        if(c_age <18)
        {
         return alert("You aren't adult");

        }
        else{
            return alert("Welcome!");
        }

       }
    </script>
<h2 class="title">Sign Up</h2>

{{> messages }}
<center>
    <div class="box">
<form action="/auth/register" method="POST">
  <div class="form-group">
    {{!-- <label for="username">Username:</label> --}}
    <input type="text" class="input name" name="username" id="username" placeholder="Username">
  </div>
  <div class="form-group">
    {{!-- <label for="password">Password:</label> --}}
    <input type="password" class="input password" name="password" id="password" placeholder="Password">
  </div>
  <div class="form-group">
    {{!-- <label for="password">Password:</label> --}}
    <input type="password" class="input" name="cnf" id="cnf" placeholder="Confirm Password">
  </div>
  <button class="button">SignUp</button>
</form>
</div>
</center>
<p style="color: aliceblue; font-size:large">Or <a href="/auth/login">Login</a> if you already have an account.</p>
</body>